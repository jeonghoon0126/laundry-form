# 이태원 출입정보 보정과 08/03 동선 문자 재발송

상태: 완료

## 요청과 의도

- 오늘 발송된 이태원 동선 문자에 사용자가 제공한 출입정보가 빠졌다.
- 08/03부터 이태원은 월·목 주 2회 동선에 반드시 포함한다.
- 최신 이태원 안내를 반영한 문자를 같은 날 1회 추가 발송한다.

## 작업 계약

- 범위: `scripts/send_route_sms.py`의 이태원 위치·출입 안내 수정.
- 범위: 08/03 경계와 월·목 주 2회 포함 여부를 회귀 테스트로 고정.
- 범위: `docs/operations.md`와 `docs/project-context-legacy.md`의 이태원 운영 기준 갱신.
- 범위: 2026/08/03 기준 수정된 LMS 동선 문자 1회 수동 발송.
- 범위: 같은 날 중복 방지를 유지하면서 명시적으로 승인된 수동 재발송만 허용하는 workflow 입력 추가.
- 범위 밖: 다른 숙소 순서·출입정보, 정산, Supabase, 자동 예약 시각·일반 발송 정책 변경.
- 사용자에게 보이는 결과: 이태원 위치, 층·문 위치, 공동현관 출입 안내가 포함된 08/03 동선 문자.
- 발송 대상: 기존 환경변수에 설정된 기사님 수신자와 표준 오너 확인 수신자(설정된 경우). 전화번호는 문서에 기록하지 않는다.
- API: Solapi 메시지 발송 API, `POST /messages/v4/send`, 운영 환경의 기존 환경변수 인증 사용.
- 발송 payload: 08/03 동선 본문과 제목의 LMS 1회 실행. 출입코드 원문은 이 문서에 복사하지 않는다.
- 실행 주체: 파일 수정·검증·요청된 1회 발송은 Codex가 수행하되, 발송 전 중복 방지 키를 확인한다.
- 롤백: 코드·운영 문서의 이태원 안내를 이전 문구로 되돌릴 수 있다. 이미 발송된 문자는 회수할 수 없다.

## 중복 방지와 실행 조건

- 승인 근거: 사용자가 최신 안내를 추가하고 `1회 수동 재발송해`라고 명시했다.
- 중복 방지 키: `route-sms-2026-08-03-itaewon-access-v2-1`
- 키 상태: 사용됨
- 키 사용 조건: 발송 전 이 문서의 상태가 `진행 중` 또는 `검토 중`이고, 발송 기록이 없어야 한다.
- 발송 후: 실제 API 결과와 수신자별 성공·실패를 확인한 뒤 키를 사용됨으로 기록한다.
- 전송 오류가 애매하게 끝나면 재시도하지 않고 결과 확인 전 대기한다.

## 검증 기준

- 수정 전 테스트를 먼저 추가한다.
- `TEST_DATE=2026-08-03` dry-run에서 새 이태원 위치·출입 안내가 보이고 이전 안내가 사라진다.
- 08/03과 이후 목요일·월요일에 이태원이 포함되고, 월·목 외 날짜에는 발송하지 않는다.
- 전체 테스트와 Python 문법 검사를 통과한다.
- 발송 전 dry-run 출력과 대상 환경변수 존재 여부를 확인하되, 비밀값·전화번호는 출력하지 않는다.
- 발송 후 API 응답, 실행 시각, 표준 수신자별 결과를 확인한다.

## 지침 적용 확인

- 적용: 저장소 `AGENTS.md`, `/Users/wjh/AGENTS.md`, 세션·API·운영 변경·문자 발송 규칙, `tdd-workflow`, `security-review`.
- 실제 문자 발송은 사용자의 명시 요청 범위에 한정한다.
- 출입코드와 전화번호는 답변·작업 문서·로그에 새로 노출하지 않는다.
- 범위가 자동 발송 설정 배포나 외부 데이터 변경으로 넓어지면 별도 계약으로 분리한다.

## 진행 기록

- 상태: 완료
- 테스트: 새 이태원 안내·주 2회 회귀 테스트 작성, 수정 전 실패 확인 완료
- 테스트: 이태원 관련 11개 테스트 통과, dry-run 제목·장소 노출 확인, 문법·공백 검사 통과
- 해소: 사용자의 1회 재발송 승인을 받아 원격 코드 반영과 수동 재발송 경로 작업을 진행함
- 코드 수정: 원격 `main` 반영 완료(PR #8, merge commit `28e40a1483052730d652d9e9bb61c785c07e8c73`)
- 수동 발송: 2026-08-03 14:09 KST, workflow run `30786339262` 성공
- 사후 확인: 수정된 08/03 동선 본문 실행, 기사님 SMS 성공, 오너 동선 SMS 성공 확인
- 마무리: cleanup PR #9 병합, 임시 `manual_resend` 우회 입력 제거 및 평상시 중복 방지 복원 확인
